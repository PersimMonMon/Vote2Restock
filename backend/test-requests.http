@USER_ID = testUser126
@ITEM_ID = product126

### Request 1: Create a vote
### Response
### Status Code: 201
### Body: JSON object for the newly created vote
POST http://localhost:3000/generateVote HTTP/1.1
content-type: application/json

{
  "userId": "{{USER_ID}}",
  "itemId": "{{ITEM_ID}}"
}

### Request 2: Retrieve total votes for the item
### Response
### Status Code: 200
### Body: Single JSON object with totalVotes
GET http://localhost:3000/votes?itemId={{ITEM_ID}} HTTP/1.1

### Request 3: Toggle the vote (switch userChoice between 0 and 1)
### Response
### Status Code: 200
### Body: JSON object with updated vote
PUT http://localhost:3000/toggle HTTP/1.1
content-type: application/json

{
  "userId": "{{USER_ID}}",
  "itemId": "{{ITEM_ID}}"
}

### Request 4: Retrieve total votes again (should now reflect the toggled userChoice)
### Response
### Status Code: 200
### Body: Single JSON object with totalVotes
GET http://localhost:3000/votes?itemId={{ITEM_ID}} HTTP/1.1
